// Module path matches the private github.com/project-empat/gatewarden-enterprise
// repository so the OSS stub can be swapped for the real implementation via
// the enterprise workspace (go.work.enterprise) or a replace directive.
module github.com/project-empat/gatewarden-enterprise

go 1.22
